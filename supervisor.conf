[supervisord]
nodaemon = true
pidfile = /run/supervisord.pid

[program:nginx]
command = /usr/sbin/nginx
user = root
autostart = true

[program:java]
command = mvn exec:java -Dexec.mainClass="presenter.rest.RESTApp"
user = root
autostart = true
